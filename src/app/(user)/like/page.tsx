import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Like Page',
  description: 'mieu ta trang like page'
}

const LikePage = () => {
  return <div>LikePage</div>;
};

export default LikePage;
